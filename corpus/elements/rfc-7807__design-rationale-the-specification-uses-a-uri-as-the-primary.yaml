document: rfc-7807
kind: design-rationale
summary: The specification uses a URI as the primary problem type identifier (rather than a code or enum) so that HTTP APIs can nominate new URIs under their control or reuse existing ones, enabling a decentralized, extensible type system without a central registry.
section: "1"
topics:
    - http
    - json
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-28"
