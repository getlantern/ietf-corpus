document: rfc-8533
kind: protocol-element
summary: 'The `rpc-input-parameters` grouping provides common input fields shared by both RPCs: a `destination-tp` container (using `cl-oam:tp-address`), mandatory `source-interface` and `outbound-interface` leaves (type `if:interface-ref`), and an optional `vrf` leaf (type `cl-oam:routing-instance-ref`).'
section: "4"
topics:
    - yang
    - netconf
    - measurement
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-05"
