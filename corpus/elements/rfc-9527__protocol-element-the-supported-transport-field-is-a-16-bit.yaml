document: rfc-9527
kind: protocol-element
summary: The Supported Transport field is a 16-bit bitmask where each bit represents a specific transport protocol; bit 0 (least significant) designates DomTLS (DNS over mutually authenticated TLS, default port 853), and bits 1–15 are unassigned.
section: "4.4"
topics:
    - dns
    - tls
    - registry
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-12"
