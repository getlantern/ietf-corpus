document: rfc-7515
kind: design-rationale
summary: Base64url encoding is used without padding ('=' characters omitted) to produce representations that are valid in URIs and HTTP headers without percent-encoding, while still allowing unambiguous decoding since the payload length is determinable from context.
section: "2"
topics:
    - jose
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-21"
