document: rfc-4516
kind: protocol-element
summary: 'Default field values when absent: `<port>` defaults to TCP 389; `<dn>` defaults to the zero-length DN ""; `<scope>` defaults to "base"; `<filter>` defaults to "(objectClass=*)"; `<attributes>` defaults to all user attributes; `<extensions>` defaults to none.'
section: "3"
topics:
    - ldap
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-16"
