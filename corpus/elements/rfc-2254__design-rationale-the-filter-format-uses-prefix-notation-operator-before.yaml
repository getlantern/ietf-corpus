document: rfc-2254
kind: design-rationale
summary: The filter format uses prefix notation (operator before operands, e.g., '(&(a=1)(b=2))') rather than infix, which is consistent with LDAP's ASN.1 structure and simplifies unambiguous recursive parsing.
section: "4"
topics:
    - ldap
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
