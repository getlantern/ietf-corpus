document: rfc-7808
kind: normative-requirement
summary: Clients configured to use the TLS-based service MUST NOT fall back to the non-TLS service if TLS is unavailable, and MUST NOT follow HTTP redirect requests from a TLS service to a non-TLS service.
section: "8"
rfc2119_level: MUST NOT
topics:
    - http
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-28"
