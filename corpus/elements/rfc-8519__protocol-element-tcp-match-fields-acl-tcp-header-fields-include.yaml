document: rfc-8519
kind: protocol-element
summary: 'TCP match fields (''acl-tcp-header-fields'') include sequence-number (uint32), acknowledgement-number (uint32), data-offset (uint8, range 5..15), reserved (uint8), flags (bits: cwr, ece, urg, ack, psh, rst, syn, fin), window-size (uint16), urgent-pointer (uint16), and options (binary, length 1..40).'
section: "4.2"
topics:
    - yang
    - tcp
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-05"
