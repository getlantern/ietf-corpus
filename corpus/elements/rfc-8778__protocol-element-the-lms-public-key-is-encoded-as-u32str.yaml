document: rfc-8778
kind: protocol-element
summary: 'The LMS public key is encoded as: u32str(lms_algorithm_type) || u32str(otstype) || I || T[1], where I is the 16-byte private key identifier and T[1] is the m-byte string associated with the root of the LMS tree.'
section: "2.2"
topics:
    - crypto
    - cbor
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-06"
