document: rfc-9691
kind: normative-requirement
summary: When converting a TAK object to a TAL file, an RP MUST default to producing a TAL based on the 'current' TAKey, MUST include any comments from the TAKey, and MUST NOT produce a TAL if TAK validation fails.
section: "7"
rfc2119_level: MUST
topics:
    - routing
    - pkix
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-12"
