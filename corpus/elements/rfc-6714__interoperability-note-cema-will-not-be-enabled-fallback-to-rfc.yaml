document: rfc-6714
kind: interoperability-note
summary: 'CEMA will not be enabled (fallback to RFC 4975 occurs) when: a non-CEMA endpoint is ''active'', a non-CEMA endpoint using a relay is ''passive'', or a CEMA endpoint using a relay becomes ''active'' (session matching would fail if c/m-line address were used in ToPath).'
section: "4.1"
topics:
    - realtime
    - sip
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-21"
