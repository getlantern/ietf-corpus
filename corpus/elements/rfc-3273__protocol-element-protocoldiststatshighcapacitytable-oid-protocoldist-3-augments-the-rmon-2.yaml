document: rfc-3273
kind: protocol-element
summary: protocolDistStatsHighCapacityTable (OID { protocolDist 3 }) augments the RMON-2 protocolDistStatsTable with ZeroBasedCounter64 packet and octet counts per protocol, noting that fragmented network-layer packets increment the counter once per link-layer frame.
section: "5"
topics:
    - snmp
    - measurement
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-25"
