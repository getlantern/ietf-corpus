document: rfc-2829
kind: normative-requirement
summary: For certificate-based authentication, the server MUST request a certificate during TLS negotiation, the client MUST perform a private key-based encryption proving possession of the private key, and the server MUST verify that the client's certificate is valid.
section: "7.1"
rfc2119_level: MUST
topics:
    - ldap
    - tls
    - pkix
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
