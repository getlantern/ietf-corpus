document: rfc-2617
kind: protocol-element
summary: 'The Digest Authorization request header includes: username, realm, nonce, digest-uri, response (32 hex digits), algorithm (optional), cnonce (required if qop present), opaque (if provided by server), message-qop (optional but SHOULD be used), and nonce-count (required if qop present).'
section: 3.2.2
topics:
    - http
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-22"
