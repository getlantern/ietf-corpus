document: rfc-2988
kind: normative-requirement
summary: When the first RTT measurement R is made, the host MUST set SRTT <- R, RTTVAR <- R/2, RTO <- SRTT + max(G, K*RTTVAR) where K=4.
section: "2.2"
rfc2119_level: MUST
topics:
    - tcp
    - congestion
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-25"
