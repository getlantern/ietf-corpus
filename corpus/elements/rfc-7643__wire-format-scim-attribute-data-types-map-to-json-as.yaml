document: rfc-7643
kind: wire-format
summary: 'SCIM attribute data types map to JSON as follows: String → JSON string (UTF-8), Boolean → JSON boolean, Decimal/Integer → JSON number, DateTime → JSON string in xsd:dateTime format, Binary → JSON string (base64-encoded), Reference → JSON string (URI), Complex → JSON object.'
section: "2.3"
topics:
    - json
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-22"
