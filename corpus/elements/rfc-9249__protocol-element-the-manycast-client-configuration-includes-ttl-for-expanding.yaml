document: rfc-9249
kind: protocol-element
summary: The manycast-client configuration includes TTL (for expanding ring search), minclock (minimum manycast survivors), maxclock (maximum manycast candidates), and beacon (upper poll limit; when TTL reaches its limit without finding minclock servers, poll increases until beacon then restarts).
section: "8"
topics:
    - yang
    - ntp
    - multicast
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-06"
