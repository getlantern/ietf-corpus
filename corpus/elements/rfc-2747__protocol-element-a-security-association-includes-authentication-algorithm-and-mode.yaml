document: rfc-2747
kind: protocol-element
summary: 'A security association includes: authentication algorithm and mode, key, key lifetime (KeyStartValid/KeyEndValid), sending interface (sender-side), source address of sender (receiver-side), latest sending sequence number (sender-side), and a list of the last N received sequence numbers (receiver-side).'
section: "2.2"
topics:
    - security
    - crypto
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
