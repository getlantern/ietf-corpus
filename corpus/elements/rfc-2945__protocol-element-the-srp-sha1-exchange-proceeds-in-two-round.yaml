document: rfc-2945
kind: protocol-element
summary: 'The SRP-SHA1 exchange proceeds in two round trips: first the client sends username U and the host responds with salt s; then the client sends A = g^a % N and the host responds with B = (v + g^b) % N. Both sides derive the shared session key S independently.'
section: "3"
topics:
    - crypto
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
