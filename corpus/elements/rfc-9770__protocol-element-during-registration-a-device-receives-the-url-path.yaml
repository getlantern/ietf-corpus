document: rfc-9770
kind: protocol-element
summary: 'During registration, a device receives: the url-path to the TRL endpoint, the hash function identifier (from Named Information Hash Algorithm Registry), MAX_N (if diff queries supported), and MAX_DIFF_BATCH (if the Cursor extension is also supported). The AS maintains registration state until deregistration.'
section: "10"
topics:
    - security
    - registry
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-19"
