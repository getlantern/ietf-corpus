document: rfc-2573
kind: protocol-element
summary: 'snmpTargetSpinLock (TestAndIncr) provides a serialization mechanism for concurrent modification of snmpTargetAddrTagList by multiple managers: retrieve spinlock and tag list, generate new tag list, then set both atomically — if the spinlock set fails, restart from step 1.'
section: 4.1.2
topics:
    - snmp
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-22"
