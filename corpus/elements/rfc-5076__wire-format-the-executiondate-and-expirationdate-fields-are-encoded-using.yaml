document: rfc-5076
kind: wire-format
summary: The `<executionDate>` and `<expirationDate>` fields are encoded using the XML Schema `date` data type. `<expirationDate>` is optional (`minOccurs="0"`) while `<executionDate>` is required in the example schema.
section: 4.5.4
topics:
    - dns
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-16"
