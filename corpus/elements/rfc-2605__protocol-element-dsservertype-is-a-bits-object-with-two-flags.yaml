document: rfc-2605
kind: protocol-element
summary: 'dsServerType is a BITS object with two flags: frontEndDirectoryServer(0) and backEndDirectoryServer(1). Both bits may be set simultaneously if a server acts as both frontend and backend.'
section: "5"
topics:
    - ldap
    - snmp
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
