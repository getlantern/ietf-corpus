document: rfc-9883
kind: normative-requirement
summary: 'For CRMF use: the `popo` field MUST use the signature CHOICE, `poposkInput` MUST be present, `POPOSigningKeyInput.authInfo` MUST use the sender CHOICE, `publicKey` MUST be a copy of the key from `certTemplate`, and the signature MUST validate with the public key in the signature certificate.'
section: "5"
rfc2119_level: MUST
topics:
    - pkix
    - crypto
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-19"
