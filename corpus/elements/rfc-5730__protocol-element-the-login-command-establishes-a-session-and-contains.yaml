document: rfc-5730
kind: protocol-element
summary: The <login> command establishes a session and contains <clID> (client identifier), <pw> (plain text password), optional <newPW> (new password), <options> with <version> and <lang> (must exactly match greeting values), and <svcs> listing <objURI> elements and optional <svcExtension>.
section: 2.9.1.1
topics:
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-22"
