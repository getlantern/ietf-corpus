document: rfc-2390
kind: protocol-element
summary: InARP uses the same packet format as standard ARP, with fields ar$hrd (16 bits, hardware type), ar$pro (16 bits, protocol type), ar$hln (8 bits, hardware address length), ar$pln (8 bits, protocol address length), ar$op (16 bits, operation code), ar$sha, ar$spa, ar$tha, and ar$tpa.
section: "6"
topics:
    - ip
    - registry
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-22"
