document: rfc-1808
kind: design-rationale
summary: The resolution algorithm in Section 4 is defined as an example against which implementations can be tested; implementation of the algorithm itself is not required. Equivalent implementations (e.g., segment stacks instead of string pattern matches) are acceptable as long as they produce the same output.
section: "4"
topics:
    - http
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
