document: rfc-2513
kind: design-rationale
summary: Object selection uses (subtree, list) tuples so that only one OID and one OCTET STRING are needed to define a subset of objects, requiring an NM application to create only one conceptual row per subset rather than one row per object.
section: "3.2"
topics:
    - snmp
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
