document: rfc-3107
kind: design-rationale
summary: The label value is placed in the NLRI rather than a BGP path attribute so that different routes to the same destination can carry distinct labels within a single Update message, enabling per-route label granularity without requiring separate messages.
section: "3"
topics:
    - bgp
    - mpls
    - routing
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-25"
