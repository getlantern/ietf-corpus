document: rfc-8997
kind: normative-requirement
summary: MUAs MUST implement TLS 1.2 or later (e.g., TLS 1.3). Earlier TLS and SSL versions MAY also be supported, so long as the MUA requires at least TLS 1.2 when accessing accounts configured to impose minimum confidentiality requirements.
section: "3"
rfc2119_level: MUST
topics:
    - tls
    - email
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-06"
