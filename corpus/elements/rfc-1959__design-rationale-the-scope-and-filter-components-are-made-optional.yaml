document: rfc-1959
kind: design-rationale
summary: The `<scope>` and `<filter>` components are made optional with well-defined defaults (`base` scope and `(objectClass=*)` filter) to allow concise LDAP URLs that reference a single known entry without requiring explicit search parameters.
section: "2"
topics:
    - ldap
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
