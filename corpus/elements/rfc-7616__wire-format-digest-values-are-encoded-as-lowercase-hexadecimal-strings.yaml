document: rfc-7616
kind: wire-format
summary: 'Digest values are encoded as lowercase hexadecimal strings: MD5 produces 32 hex characters (128 bits), SHA-256 produces 64 hex characters (256 bits), and SHA-512/256 produces 64 hex characters (256 bits). Bits are converted most-significant to least-significant, four bits at a time.'
section: "3.2"
topics:
    - http
    - crypto
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-22"
