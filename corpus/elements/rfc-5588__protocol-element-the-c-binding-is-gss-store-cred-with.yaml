document: rfc-5588
kind: protocol-element
summary: 'The C-binding is gss_store_cred() with signature: OM_uint32 gss_store_cred(OM_uint32 *minor_status, gss_cred_id_t input_cred_handle, gss_cred_usage_t cred_usage, const gss_OID desired_mech, OM_uint32 overwrite_cred, OM_uint32 default_cred, gss_OID_set *elements_stored, gss_cred_usage_t *cred_usage_stored). The boolean arguments overwrite_cred and default_cred are typed as OM_uint32, with 0=FALSE and non-zero=TRUE.'
section: "4"
topics:
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-06-17"
