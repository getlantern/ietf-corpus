document: rfc-2948
kind: protocol-element
summary: The DES3 OFB64 algorithm applies three DES operations in sequence — encrypt with key1, decrypt with key2, encrypt with key3 — to the feedback register to produce each keystream block, which is then XORed with plaintext.
section: "4"
topics:
    - crypto
    - security
extracted_by: claude-sonnet-4-6
extracted_at: "2026-05-24"
