document: rfc-9679
kind: design-rationale
summary: Optional COSE Key parameters are excluded from the thumbprint computation so that the same underlying key always produces the same thumbprint regardless of which optional attributes (e.g., 'kid', usage constraints) accompany it in different application contexts.
section: "5.1"
topics:
    - cbor
    - crypto
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-12"
