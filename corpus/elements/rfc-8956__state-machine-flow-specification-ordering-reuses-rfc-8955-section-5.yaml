document: rfc-8956
kind: state-machine
summary: 'Flow Specification ordering reuses RFC 8955 Section 5.1 rules with added IPv6 offset consideration: if offsets are equal, longest-prefix-match semantics apply; if offsets differ, the lower offset value takes precedence because it matches the most significant bit position.'
section: "4"
topics:
    - routing
    - bgp
    - ip
extracted_by: claude-sonnet-4-6
extracted_at: "2026-07-06"
