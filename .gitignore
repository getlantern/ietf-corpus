# Cached plaintext (regenerable from rfc-editor.org).
# Kept locally to avoid re-fetching on extraction re-runs, but excluded
# from git to keep the repo manageable as the corpus grows.
/corpus/text/

# Cached source files (regenerable from upstream)
/tmp/
*.cache
.DS_Store

# Build artifacts
/ietf-crawl
/ietf-mcp
/ietf-site
/dist/
